import express from "express"
import axios from "axios"
import encodeUrl from "encodeurl"

const findBestRoute = (address: string[]) => {
    let url = "https://maps.googleapis.com/maps/api/distancematrix/json?"


}
